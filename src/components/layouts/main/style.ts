import { makeStyles } from "@material-ui/core"

export const useStyles = makeStyles({
  fullHeight: {
    height: "100%"
  },
  content: {
    flexGrow: 1
  },
  contentWrapper: {
    display: "flex",
    overflow: "hidden"
  }
})
